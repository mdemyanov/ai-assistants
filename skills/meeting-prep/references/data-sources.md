# Источники данных

Где искать информацию для подготовки к встречам. Skill универсален — структуру vault получает из системного промта ассистента.

## Определение параметров окружения

### Шаг 1: Получи vault_name

**Из системного промта** — ищи секции:
- "База знаний"
- "Инструменты" 
- "aigrep"
- Упоминание конкретного vault

**Если не указан:**
```
aigrep:list_vaults
```

### Шаг 2: Определи структуру папок

**Из системного промта** — ищи:
- Таблицу структуры папок
- Описание "где хранить"
- Примеры путей

**Если не указана — определи через поиск:**
```
# Найти где хранятся люди
search_vault([vault], "type:person", limit=3)
# → посмотри путь в результатах

# Найти где хранятся проекты  
search_vault([vault], "type:project", limit=3)
```

**Или спроси пользователя:**
> "В какой папке vault хранятся профили людей?"

## API поиска: aigrep

### Базовый синтаксис

```
aigrep:search_vault
  vault_name: [из системного промта]
  query: "текст запроса"
  search_type: "hybrid"         # hybrid | fts | vector
  limit: 10
```

### Типы поиска

| Тип | Когда использовать |
|-----|-------------------|
| `hybrid` | По умолчанию. Сочетает семантику и текст |
| `fts` | Точный поиск по имени, термину |
| `vector` | Семантический поиск по смыслу |

### Фильтры в query

| Фильтр | Синтаксис | Пример |
|--------|-----------|--------|
| По типу | `type:тип` | `type:1-1`, `type:person`, `type:adr` |
| По связям | `links:заметка` | `links:ivanov` |
| По тегам | `tags:тег1,тег2` | `tags:python,async` |
| По дате создания | `created:>YYYY-MM-DD` | `created:>2024-12-01` |
| По дате изменения | `modified:>=YYYY-MM-DD` | `modified:>=2024-12-01` |
| Диапазон дат | `created:>... created:<...` | `created:>2024-12-01 created:<2024-12-31` |

Фильтры комбинируются через пробел (AND).

## Типичные структуры vault

Структура определяется из секции "Контекст для skills" в CLAUDE.md проекта.

### Универсальная структура C-level (v3.0)

```
vault/
├── {people_dir}/            # Профили людей
│   └── {id}/
│       ├── profile.md       # Профиль человека
│       └── 1-1/             # История встреч
├── {projects_dir}/          # Активные проекты
│   └── {id}/
│       ├── INDEX.md
│       └── meetings/
├── {committees_dir}/        # Комитеты
└── {templates_dir}/         # Шаблоны
```

**Пример для Naumen CTO (v3.0):**
```
Naumen_CTO/
├── 10_PEOPLE/               # people_dir
│   └── amuratov/
│       ├── profile.md
│       └── 1-1/
├── 30_PROJECTS/active/      # projects_dir
│   └── psb/
│       └── INDEX.md
├── 20_MEETINGS/committees/  # committees_dir
└── 90_TEMPLATES/            # templates_dir
```

### Минимальная структура

```
vault/
├── people/                  # people_dir
│   └── {id}/
│       └── profile.md
└── meetings/
```

## Профиль человека (profile.md)

### Типичные секции для повестки

| Секция | Содержит | Использование |
|--------|----------|---------------|
| `📌 Вопросы для следующей 1-1` | Готовые темы | → Мои темы |
| `📝 Открытые договорённости` | Задачи со сроками | → Review |
| `Текущие цели` | Приоритеты | → Контекст тем |
| `Зоны развития` | Над чем работает | → Deep-режим |
| `Подчинённые` | Люди в команде | → Темы по команде |

### Типичный формат договорённостей

```markdown
| Дата | Договорённость | Срок | Статус |
|------|----------------|------|--------|
| 08.12 | Прислать документ | Эта неделя | ⬜ |
```

Статусы: ⬜ открыто, 🔄 в работе, ✅ закрыто

### Поиск профиля

```
# По имени (точный)
search_vault([vault], "[Фамилия]", search_type="fts")

# По типу
search_vault([vault], "type:person [Имя]", search_type="hybrid")
```

## История 1-1 встреч

**Паттерн пути:** `{people_dir}/{id}/1-1/`

**Пример для Naumen CTO:** `10_PEOPLE/amuratov/1-1/`

| Файл | Содержит |
|------|----------|
| `YYYY-MM-DD.md` | Заметки встречи |
| `YYYY-MM-DD_agenda.md` | Подготовленная повестка |
| `YYYY-MM-DD_analysis.md` | Анализ коммуникации |

**Из последней встречи извлекай:**
- Договорённости → Review
- "Следующая встреча: темы" → Повестка
- Заметки → Контекст

### Поиск истории 1-1

```
# Все 1-1 с человеком
search_vault([vault], "type:1-1 links:[имя]", search_type="hybrid", limit=5)

# За последний месяц
search_vault([vault], "type:1-1 links:[имя] created:>[дата месяц назад]", limit=5)
```

## Проекты

**Паттерн пути:** `{projects_dir}/{id}/`

**Пример для Naumen CTO:** `30_PROJECTS/active/psb/`

### Ключевые секции

| Секция | Для повестки |
|--------|--------------|
| Frontmatter | status, priority, deadline |
| Текущий статус | Что сделано, блокеры |
| Задачи | Что в работе |
| Риски | Что обсудить |
| Команда | Кого пригласить |

### Поиск проектов

```
# По названию
search_vault([vault], "[название] проект", search_type="hybrid")

# По связи с человеком
search_vault([vault], "links:[имя] type:project", search_type="hybrid")

# Активные проекты
search_vault([vault], "tags:project,active", search_type="hybrid")
```

## Анализы встреч (*_analysis.md)

Генерируются skill'ом `meeting-insights-analyzer`.

**Для deep-режима извлекай:**

| Секция | Применение |
|--------|------------|
| Зоны развития | Фокус на встрече |
| Ключевые наблюдения | Паттерны коммуникации |
| На следующую встречу | Конкретные рекомендации |

### Поиск анализов

```
search_vault([vault], "[имя] analysis паттерны", search_type="vector", limit=3)
```

## Алгоритм сбора данных

### Для 1-1

1. `search_vault([vault], "[имя]", search_type="fts")` — профиль
2. Извлечь вопросы и договорённости из profile.md
3. `search_vault([vault], "type:1-1 links:[имя]", limit=3)` — последние встречи
4. [Deep] `search_vault([vault], "[имя] analysis", search_type="vector")` — анализы
5. `search_vault([vault], "links:[имя] type:project")` — проекты

### Для проектной встречи

1. `search_vault([vault], "[проект]", search_type="hybrid")` — INDEX.md
2. Извлечь статус, блокеры, задачи
3. Для каждого участника → проверить profile

### Для комитета

1. `search_vault([vault], "[комитет] type:committee")` — папка комитета
2. `search_vault([vault], "type:meeting [комитет] created:>...", limit=3)` — история
3. Найти связанные проекты и решения
4. Подготовить позицию по темам
