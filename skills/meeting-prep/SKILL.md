---
name: meeting-prep
description: Подготовка к встречам — создаёт файл повестки на основе данных vault. Используй при "подготовь к встрече", "готовлюсь к 1-1", "нужна повестка", "agenda для [встречи]". Собирает договорённости, цели, блокеры, insights из профилей и истории. Поддерживает любые форматы 1-1, проектные, комитеты.
---

# Подготовка к встречам

Создаёт файл повестки в vault на основе актуальных данных: профили, проекты, история встреч, insights из анализов.

## Core Principles

**Результат — файл, не чат.** Итог работы = сохранённый .md в правильной папке vault.

**Данные важнее формы.** Структура адаптируется под контекст. Главное — собрать: договорённости, цели, блокеры, темы.

**Универсальность.** Skill работает с любым vault. Структуру и параметры получает из системного промта или определяет автоматически.

## Резолвинг контекста

> **Цель:** Определить vault_name, people_dir, projects_dir, committees_dir из контекста проекта.

### Алгоритм (по приоритету)

1. **Секция "Контекст для skills" в CLAUDE.md** (рекомендуемый)
   - Ищи `## Контекст для skills`
   - Извлеки параметры из таблиц: `vault_name`, `people_dir`, `projects_dir`, `committees_dir`

2. **Секция структуры в CLAUDE.md** (fallback)
   - Ищи `## Структура базы знаний` или `## Структура проекта`
   - Определи папки по паттернам названий:
     - `*PEOPLE*` или `*people*` → people_dir
     - `*PROJECT*` или `*projects*` → projects_dir
     - `*COMMITTEE*` или `*committee*` → committees_dir

3. **Автоопределение через MCP** (если нет CLAUDE.md)
   - `aigrep:list_vaults` → vault_name
   - `search_vault(query="type:person", limit=1)` → people_dir из пути результата
   - `search_vault(query="type:project", limit=1)` → projects_dir из пути

4. **Вопрос пользователю** (крайний случай)
   - "Укажите имя vault"
   - "В какой папке хранятся профили людей?"

### Параметры

| Параметр | Описание | Пример (Naumen CTO) |
|----------|----------|---------------------|
| `vault_name` | Имя vault в MCP | Naumen_CTO |
| `people_dir` | Путь к профилям людей | 10_PEOPLE |
| `projects_dir` | Путь к проектам | 30_PROJECTS/active |
| `committees_dir` | Путь к комитетам | 20_MEETINGS/committees |

**После резолвинга** — подставляй реальные пути вместо плейсхолдеров `{people_dir}` и т.д.

## Процесс

### 1. Определи контекст встречи

| Параметр | Обязательно | Источник |
|----------|-------------|----------|
| Тип встречи | Да | Из запроса или уточнить |
| Участники | Да | Из запроса → найти в vault |
| Дата | Да | Из запроса, default = завтра |
| Глубина | Нет | standard / deep по запросу |

### 2. Собери данные через aigrep

> 📖 Карта источников: `references/data-sources.md`

#### Поиск профиля человека

```
aigrep:search_vault
  vault_name: [vault_name]           # Из системного промта
  query: "[имя] type:person"
  search_type: "hybrid"
  limit: 3
```

Или точный поиск по имени:
```
aigrep:search_vault
  vault_name: [vault_name]
  query: "[фамилия]"
  search_type: "fts"
  limit: 5
```

#### Поиск истории 1-1

```
aigrep:search_vault
  vault_name: [vault_name]
  query: "type:1-1 links:[имя]"
  search_type: "hybrid"
  limit: 5
```

Или за последний месяц:
```
aigrep:search_vault
  vault_name: [vault_name]
  query: "type:1-1 links:[имя] created:>[месяц назад]"
  limit: 5
```

#### Поиск анализов встреч (deep-режим)

```
aigrep:search_vault
  vault_name: [vault_name]
  query: "[имя] analysis паттерны коммуникация"
  search_type: "vector"
  limit: 3
```

#### Поиск связанных проектов

```
aigrep:search_vault
  vault_name: [vault_name]
  query: "links:[имя] type:project"
  search_type: "hybrid"
  limit: 5
```

**Ключевые секции в профилях людей:**
- `📌 Вопросы для следующей 1-1` → темы
- `📝 Открытые договорённости` → review
- `Текущие цели` → контекст
- `Зоны развития` → deep-режим

**Deep-режим добавляет:**
- Файлы `*_analysis.md` — паттерны коммуникации
- Рекомендации по формулировкам из прошлых insights

### 3. Сформируй повестку

> 📖 Специфика типов: `references/meeting-types.md`

**Универсальная структура:**

```markdown
# [Тип] с [Участник]

**Дата / Формат / Статус**

## 1. Check-in
## 2. Review договорённостей  
## 3. Темы [участника]
## 4. Мои темы
## 5. Блокеры
## 6. Договорённости (заполняется на встрече)
## 7. Заметки
```

**Адаптация по типу:**
- **1-1:** фокус на развитии, открытые вопросы
- **Проектная:** статус, блокеры, решения, сроки
- **Комитет:** позиция, аргументы, варианты решений

**Принципы наполнения:**
- Тема = контекст (почему важно) + конкретные вопросы
- Wikilinks `[[путь]]` на связанные документы
- Пустые `- [ ]` для заполнения на встрече

### 4. Сохрани файл

**Путь определяется из контекста проекта (см. "Резолвинг контекста"):**

| Тип | Паттерн пути |
|-----|--------------|
| 1-1 | `{people_dir}/{id}/1-1/YYYY-MM-DD_agenda.md` |
| Проектная | `{projects_dir}/{проект}/meetings/YYYY-MM-DD_{тема}.md` |
| Комитет | `{committees_dir}/{комитет}/YYYY-MM-DD_{тема}/prep.md` |

**Примеры для Naumen CTO (v3.0):**
- 1-1 с Муратовым: `10_PEOPLE/amuratov/1-1/2026-01-17_agenda.md`
- Проектная PSB: `30_PROJECTS/active/psb/meetings/2026-01-17_status.md`

**Если путь неочевиден** — уточни у пользователя.

### 5. Валидация

Перед сохранением:
- [ ] Все открытые договорённости включены
- [ ] Темы имеют контекст (почему важно сейчас)
- [ ] Вопросы конкретные, не размытые
- [ ] Wikilinks корректны
- [ ] Путь соответствует структуре vault

### 6. Завершение

- Покажи summary: сколько тем, ключевые вопросы
- Напомни о просроченных договорённостях
- Предложи связанные документы

## Пример готовой повестки

```markdown
---
type: 1-1
person: "[[профиль человека]]"
date: YYYY-MM-DD
status: planned
---

# 1-1 с [Имя]

**Дата:** [дата]  
**Формат:** онлайн / офлайн

---

## 1. Check-in

- 

---

## 2. Review договорённостей

| Задача | Срок | Статус |
|--------|------|--------|
| [задача из профиля] | [срок] | ❓ |

---

## 3. Мои темы

### 3.1. [Тема]

**Контекст:** [почему важно сейчас]

**Вопросы:**
- [ ] [конкретный вопрос]

---

## 4. Блокеры

- [ ] Что сейчас больше всего мешает?

---

## 5. Договорённости

- [ ] 

---

## 6. Заметки

> 
```

## Ограничения

- Качество зависит от актуальности данных в vault
- Календарь недоступен — дату указывать явно
- Insights требуют наличия `*_analysis.md`
- Структуру vault skill определяет из секции "Контекст для skills" в CLAUDE.md проекта
